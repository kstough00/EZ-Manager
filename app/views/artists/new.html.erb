<% if flash[:message] %>
<%= flash[:message] %>
<% end %>

<%= form_for(@artist) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :performances %>
    <%= f.text_field :performances %>
    <%= collection_select :performances, :stage_id, Stage.all, :id, :name %>
    <%= f.submit %>
    <% end %>