<div class="header">
    <div class="title">EZ Manager</div>
    <% if current_user %>
        <div class="username">
            <p>Welcome, <%= current_user.username %></p>
        </div>
    <% end %>
    <div class="nav">
        <% if logged_in? %>
            <div><a href="/stages">My Stages</a></div>
            <div><a href="/artists">My Artists</a></div>
            <div><a href="/logout">Log Out</a></div>
        <% else %>
            <div><a href="/signup">Sign Up</a></div>
            <div><a href="/login">Log In</a></div>
        <% end %>
    </div>
</div>

